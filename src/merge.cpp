#include "merge.h"
#include "point.h"

#include <algorithm>

vector<Point> getSorted(vector<Point> poly1, vector<Point> poly2) {
//    auto comp = [](Point &a, Point &b){ return a.x < b.x; };
//    vector<Point>::iterator min1 = std::min_element(poly1.begin(), poly1.end());
//    vector<Point>::iterator min2 = std::min_element(poly2.begin(), poly2.end());
//    Point min = comp(*min1, *min2) ? *min1 : *min2;
}
